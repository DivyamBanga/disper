<Window x:Class="Disper.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Disper.Overlay"
        Title="Disper"
        Width="360" Height="100"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False" ResizeMode="NoResize"
        Focusable="False" IsHitTestVisible="False"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Grid>
        <Border x:Name="Pill"
                Height="44" MinWidth="128"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                CornerRadius="22"
                Background="#EE101012"
                BorderBrush="#24FFFFFF" BorderThickness="1"
                Padding="18,0"
                RenderTransformOrigin="0.5,0.5"
                Opacity="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="26" ShadowDepth="8" Direction="270" Opacity="0.45" Color="Black" />
            </Border.Effect>
            <Border.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="PillScale" ScaleX="0.96" ScaleY="0.96" />
                    <TranslateTransform x:Name="PillShift" Y="10" />
                </TransformGroup>
            </Border.RenderTransform>
            <Grid>
                <local:BarsView x:Name="Bars" Width="92" Height="26"
                                HorizontalAlignment="Center" VerticalAlignment="Center" />
                <TextBlock x:Name="Message"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontFamily="Segoe UI Variable Text, Segoe UI" FontSize="13" FontWeight="Medium"
                           Foreground="#E6FFFFFF" Opacity="0" />
                <TextBlock x:Name="Check" Text="&#xE73E;"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="16"
                           Foreground="#FFFFFFFF" Opacity="0" RenderTransformOrigin="0.5,0.5">
                    <TextBlock.RenderTransform>
                        <ScaleTransform x:Name="CheckScale" ScaleX="0.6" ScaleY="0.6" />
                    </TextBlock.RenderTransform>
                </TextBlock>
                <Ellipse x:Name="LockDot" Width="6" Height="6"
                         HorizontalAlignment="Left" VerticalAlignment="Center" Margin="-6,0,0,0"
                         Fill="#FFFFFFFF" Opacity="0" />
            </Grid>
        </Border>
    </Grid>
</Window>
