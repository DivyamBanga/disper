<Window x:Class="Disper.Dashboard.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Width="380" SizeToContent="Height" WindowStyle="None" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource B.BgElevated}"
        TextOptions.TextFormattingMode="Ideal">
    <WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="0" CornerRadius="0" GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>
    <Border BorderBrush="{DynamicResource B.Border}" BorderThickness="1" CornerRadius="14">
        <StackPanel Margin="24,22">
            <TextBlock x:Name="TitleText" Style="{StaticResource T.H2}" />
            <TextBlock x:Name="MessageText" Style="{StaticResource T.Secondary}" Margin="0,8,0,0" TextWrapping="Wrap" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
                <Button x:Name="CancelBtn" Style="{StaticResource Btn.Secondary}" Content="Cancel" Width="96" Click="OnCancel" />
                <Button x:Name="ConfirmBtn" Style="{StaticResource Btn.Primary}" Content="Confirm" Width="110" Margin="10,0,0,0" Click="OnConfirm" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
